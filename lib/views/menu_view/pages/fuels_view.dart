import 'package:flutter/material.dart';

class FuelsView extends StatelessWidget {
  const FuelsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Akaryakıtlar"),
    );
  }
}