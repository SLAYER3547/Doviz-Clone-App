import 'package:flutter/material.dart';

class AddAlarmsView extends StatelessWidget {
  const AddAlarmsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Alarm Ekle'),
    );
  }
}
