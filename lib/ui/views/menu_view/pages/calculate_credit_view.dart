import 'package:flutter/material.dart';

class CalculateCreditView extends StatelessWidget {
  const CalculateCreditView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kredi Hesaplama'),
    );
  }
}
